apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fwv-central-api-chart.fullname" . }}-test-connection"
  labels:
    {{- include "fwv-central-api-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fwv-central-api-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
